apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

namePrefix: prod-

commonLabels:
  environment: prod

replicas:
  - name: guestbook-ui
    count: 3

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: guestbook-ui
      spec:
        template:
          spec:
            containers:
            - name: guestbook-ui
              resources:
                requests:
                  memory: "128Mi"
                  cpu: "200m"
                limits:
                  memory: "256Mi"
                  cpu: "500m"
  - patch: |-
      apiVersion: v1
      kind: Service
      metadata:
        name: guestbook-ui
      spec:
        type: LoadBalancer

configMapGenerator:
  - name: guestbook-config
    literals:
      - ENV=production
      - LOG_LEVEL=warn
